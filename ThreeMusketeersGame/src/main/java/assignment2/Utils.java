package assignment2;

/**
 * Contains functions which are useful for validating user inputs
 */
public class Utils {

    /**
     * Converts index to alphabet letter
     *
     * @param i index
     * @return alphabet letter at index
     */
    public static char convertIntToLetter(int i) {
        return (char) (i + 64);
    }
}
